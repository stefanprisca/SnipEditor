//convert $int to String
${import(java.util.ArrayList)}


String ${freeName(str)} = Integer.toString(${int});

//Convert $ArrayList to array
${elemType(ArrayList[])} ${freeName(a)} = ${ArrayList}.toArray(new ArrayList());//[${ArrayList}.size]->not working!

//Convert $double to String
String ${freeName(str)} = Double.toString(${double});
